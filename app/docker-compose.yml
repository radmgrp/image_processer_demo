version: '3.7'

services:

  backend:
    build: ./backend
    ports:
      - 5000:5000
    volumes:
      - ./backend:/app
    command: uvicorn main:app --reload --host 0.0.0.0 --port 5000
    environment:
      - ALLOWED_ORIGINS=0.0.0.0:3000


